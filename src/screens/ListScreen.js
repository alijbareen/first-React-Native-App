import React, { Component } from "react";
import { Text, StyleSheet, View } from "react-native";

const ListScreen = () => {
  return <Text style={styles.textStyle}>List</Text>;
};

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 45,
  },
});

export default ListScreen;
